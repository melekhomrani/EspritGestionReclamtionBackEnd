spring:
  datasource:
    url: ${DB_STRING}
    username: ${DB_USER}
    password: ${DB_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    database: oracle
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
server:
  port: 8085
FRONT_URL: localhost:3000
